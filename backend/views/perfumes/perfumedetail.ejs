<!DOCTYPE html>
<html lang="vi">

<head>
  <meta charset="UTF-8">
  <title>
    <%= perfume.name %> - Chi tiết nước hoa
  </title>
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <style>
    body {
      background-color: #0c0c0c;
      color: #fff;
      font-family: 'Poppins', sans-serif;
      margin: 0;
      padding: 0;
    }
    .page-container {
      margin-left: 200px;
    }
    .perfume-container {
      margin-left: 200px;
      margin-top: 200px;
      max-width: 900px;
      background: #151515;
      padding: 30px;
      border-radius: 10px;
      box-shadow: 0 0 15px rgba(255, 0, 50, 0.2);
    }

    .perfume-detail {
      display: flex;
      gap: 40px;
      align-items: flex-start;
    }

    .perfume-detail img {
      width: 350px;
      height: 400px;
      object-fit: cover;
      border-radius: 10px;
      border: 1px solid #222;
    }

    .info {
      flex: 1;
    }

    .info h2 {
      font-size: 28px;
      color: #c41e3a;
      margin-bottom: 10px;
    }

    .info p {
      margin: 8px 0;
      color: #ddd;
    }

    .info strong {
      color: #fff;
    }

    .price {
      color: #c41e3a;
      font-weight: 700;
      font-size: 20px;
      margin-top: 15px;
    }

    .actions {
      margin-top: 30px;
    }

    .btn {
      display: inline-block;
      margin-right: 10px;
      background: #c41e3a;
      color: #fff;
      text-decoration: none;
      padding: 10px 16px;
      border-radius: 6px;
      transition: 0.3s;
    }

    .btn:hover {
      background: #a0142e;
    }
  </style>
</head>

<body>
  <%- include('../partials/admindashboard') %>

    <div class="page-container">
      <div class="perfume-container">
        <div class="perfume-detail">
          <img src="<%= perfume.image || '/images/no-image.png' %>" alt="<%= (perfume.perfumeName || perfume.name) %>">

          <div class="info">
            <h2>
              <%= (perfume.perfumeName || perfume.name) %>
            </h2>
            <p><strong>Brand:</strong>
              <%= (perfume.brand && (perfume.brand.brandName || perfume.brand.name)) || perfume.brand || '—' %>
            </p>
            <p><strong>URI:</strong> <%= perfume.uri || '—' %></p>
            <p><strong>Concentration:</strong> <%= perfume.concentration || '—' %></p>
            <p><strong>Volume:</strong> <%= perfume.volume ? (perfume.volume + ' ml') : '—' %></p>
            <p><strong>Target:</strong> <%= perfume.targetAudience || perfume.gender || '—' %></p>
            <p><strong>Description:</strong>
              <%= perfume.description || 'No description' %>
            </p>
            <% if (perfume.ingredients) { %>
              <p><strong>Ingredients:</strong> <%= perfume.ingredients %></p>
            <% } %>
            <p class="price">
              <%= perfume.price.toLocaleString() %> VND
            </p>

            <div class="actions">
              <a href="/perfumes/edit/<%= perfume._id %>" class="btn"><i class="fa-solid fa-pen-to-square"></i> Edit</a>
              <a href="/perfumes/delete/<%= perfume._id %>" class="btn" style="background:#700;"><i
                  class="fa-solid fa-trash"></i> Delete</a>
              <a href="/perfumes" class="btn" style="background:#333;"><i class="fa-solid fa-arrow-left"></i> Back</a>
            </div>
          </div>
        </div>
      </div>
    </div>


</body>

</html>